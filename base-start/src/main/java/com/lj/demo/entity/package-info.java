/**
 * 存放所有实体信息
 * 
 *  -- model: 存放数据库实体,建议与数据库名称一致。
 *  -- dto: 存放所有请求接参实体，如`controller`层接收参数,该文件下所有对象 建议以`DTO`结尾。
 *  -- bo: 存放业务对象，如`service`层返回包装对象，该文件下所有对象 建议以`BO`结尾。
 *  -- vo: 返回实体信息，如`controller`返回的对象，该文件下所有对象 建议以`VO`结尾。
 *  原则上建议 以上四个包对于同一业务对象命名一致。
 *
 * Created by lijun on 2019/6/4
 */
package com.lj.demo.entity;