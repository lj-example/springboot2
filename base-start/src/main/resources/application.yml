server:
  port: 8080
  servlet:
    context-path: /t

spring:
    profiles:
      active: dev
    application:
      name: base-start

debug: true



logging:
  config: "classpath:config/log4j2-${spring.profiles.active}.xml"

pagehelper:
  reasonable: true
  support-methods-arguments: true
